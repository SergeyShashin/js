'use strict';

/*
Вывести с помощью цикла for числа от 0 до 9, НЕ используя тело цикла. То есть
выглядеть должно вот так:
```
for(…){/* здесь пусто */

for (let number = 0; number < 10; console.log(number), number++) { }